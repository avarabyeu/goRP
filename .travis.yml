sudo: false
language: go
go:
  - "1.12.x"

env:
  - GO111MODULE=on

before_script:
  - make init-deps

script:
  - make build

after_success:
 - curl --request POST "https://goreportcard.com/checks" --data "repo=github.com/avarabyeu/goRP"
